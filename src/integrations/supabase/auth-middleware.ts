// Auth middleware is intentionally a no-op in this Vite/React SPA setup.
// The original file referenced TanStack Start packages that are not installed
// in this project. This stub keeps the module resolvable for any stale imports.
export const requireSupabaseAuth = null as unknown as never;
